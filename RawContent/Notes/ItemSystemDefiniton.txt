**Full Description of the Intended Item System**  
*(Updated 2026-08-13 – post hold-system + fragment design lock-in)*

### 1. High-Level Goals
- Fully data-driven item definitions using Primary Data Assets + Fragments.
- One `UItemInstance` = one physical item in the world (no stacking of instances).
- World representation is always an `AItemActor` that is never destroyed on pickup.
- Clean separation of concerns:
  - **Data** → Definitions + Fragments
  - **Runtime state** → `UItemInstance`
  - **World presence** → `AItemActor`
  - **Player holding logic** → `UHeldItemsComponent`
  - **Presentation** → Animation, Control Rig, Physics Control (mostly Blueprint + component)

### 2. Core Code Types

**UItemDefinition** (Primary Data Asset)  
Contains an array of `UItemFragment*` instances.  
This is the pure data definition of an item type (Stick, Axe, Unarmed, etc.).

**UItemFragment** (abstract base)  
Lightweight, instanced, edit-inline objects that add specific capabilities.  
Examples: Equippable, Durability, Fuel, Damage, etc.

- Fragments are **primarily pure data**.
- Data is exposed as `UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)`.
- Allowed exception: a lightweight virtual `OnItemSpawned(AItemActor* ItemActor)` for one-time initialization only.
- `OnItemSpawned` may set initial runtime values (e.g. CurrentHealth = MaxDurability) or spawn a matching runtime component when ongoing behaviour is truly needed.
- Fragments must **not** hold mutable runtime state or perform per-frame work.

**UItemInstance**  
Runtime object that holds a pointer to its `UItemDefinition` and any per-instance mutable state (CurrentHealth, UniqueId, etc.).  
The Definition pointer is kept for the lifetime of the instance (UI, repair, tooltips, save/load, etc.).  
Created by the factory. Never stacked.

**AItemActor**  
The actual actor in the world.  
- Owns a static mesh component.  
- Holds a reference to its `UItemInstance`.  
- `InitializeFromInstance()` sets the mesh and calls `OnItemSpawned` on every fragment.  
- Survives pickup/drop cycles (never destroyed on pickup).

**UItemFactory**  
Responsible for creating the `UItemInstance`, spawning the `AItemActor`, and handing the instance to the actor.  
Stays thin — real configuration happens inside `AItemActor::InitializeFromInstance`.

**EHand** (in `WoodcraftTypes.h`)  
```cpp
UENUM(BlueprintType)
enum class EHand : uint8
{
	Left,
	Right,
	None
};
```

### 3. Equippable System & Collision

**UEquippableFragment**  
Currently minimal (`bTwoHanded`).  
In `OnItemSpawned` it configures the actor to respond to the Equippable trace channel

**Collision Channels (locked approach)**  
- Every item uses Object Type = `ITEM`.  
- Equippable items additionally set:
  ```cpp
  Mesh->SetCollisionResponseToChannel(COLLISION_EQUIPPABLE, ECR_Overlap);
  ```
- Pickup uses a **Trace Channel** called `EQUIPPABLE` so the sphere sweep only hits valid equippable items.
- This keeps the ITEM object channel free for other interactions while giving clean targeting for pickup.

**Hand Ownership**  
- One primary hand owns the item.  
- `bTwoHanded == true` → opposite hand becomes secondary.  
- Player can hold two different one-handed items at the same time.

### 4. Animation & Attachment Architecture (Locked)

Two skeletal meshes on the player:

- **FPArmsMeshAnimRef** (invisible, no collision, truth source)  
  Pure animation authority. Plays montages and hold poses. Zero IK.  
  Physics Control is driven from `WeaponBone_L` / `WeaponBone_R`.

- **FPArmsMeshVisible** (visible)  
  Copies pose from AnimRef, then Control Rig IK to grip targets supplied by `UHeldItemsComponent`.

**Physics Control**  
- One `UPhysicsControlComponent` on the character.  
- Drives the held item relative to the corresponding AnimRef WeaponBone.  
- Gives real momentum while still following the hand.

**Dynamic Grip Transform** (every frame)  
```
Relative = AnimRef.HandBone.GetRelativeTransform(AnimRef.WeaponBone)
GripTarget = Relative * ItemWorldTransform
```
Left-hand IK works without extra transform mirroring.

**Left-hand item mesh mirroring (implemented)**  
- Only the mesh component is scaled: `SetRelativeScale3D(FVector(-1.f, 1.f, 1.f))` when held in left hand.  
- Scale is forced back to `(1,1,1)` on detach.  
- Actor root scale stays positive (avoids Physics Control issues).

### 5. Held Item Safety (implemented)

`PreventItemStuck(EHand)` runs every tick from `UHeldItemsComponent`.

- Measures distance between AnimRef **HandBone** and the current **GripLocation** (more accurate than WeaponBone→item for long weapons).
- Hysteresis (enter ~55, exit ~40 — tune as needed).
- When stuck: changes collision **responses** on WorldStatic, WorldDynamic, and custom channels (macros) COLLISION_ITEM and soon COLLISION_STRUCTURE and COLLISION_CREATURE to `Ignore`.  
- When free: restores them to `Block`.  
- **Never** uses `SetCollisionEnabled(NoCollision)` — that invalidates Physics Control bodies.
- light Control Rig stretch on the visible arms complement so the hand stays visually glued without the elbows snapping in when item pulled away from player.

### 6. UHeldItemsComponent Responsibilities

- Manages both hands (always a valid item — Unarmed when empty).
- `TryPickup` / `TryDrop`.
- Creates / destroys Physics Controls on attach / detach.
- Dynamic grip transform calculation every frame.
- Left-hand mesh mirroring.
- `PreventItemStuck` safety.
- Will also own the targeted-item query and (for now) highlight driving logic.
- Provides clean Blueprint API.

Note: The component is currently large (many helpers). This is accepted while the hold/pickup systems are in active development. Public API stays small; helpers stay private. Split later only when stable groups of responsibility appear.

### 7. Player Structure

- C++ `AWoodcraftCharacter` owns the `UHeldItemsComponent`.
- WoodcraftPlayer Blueprint subclasses it.
- C++ owns state and rules; Blueprint owns presentation (animation, Control Rig, Physics Control tuning).

### 8. Unarmed Handling (Locked)

Unarmed is a real item (`DA_Unarmed`).  
Both hands start with Unarmed.  
Dropping an item immediately equips Unarmed in that hand.  
Eliminates null-hand special cases.

### 9. Current Status (2026-08-13)

**Working / Implemented**
- Item Factory + spawning + `InitializeFromInstance`
- Fragment `OnItemSpawned` loop (data-driven init)
- Physics Control attach + dynamic grip (both hands)
- Left-hand mesh mirroring (scale X = -1 on mesh only)
- `PreventItemStuck` (response-based, HandBone↔Grip distance, hysteresis)
- Basic hold feel

**In progress / Next**
1. Basic `TryPickup` / `TryDrop` using sphere sweep on the `EQUIPPABLE` Trace Channel (so mirroring can be fully tested).
2. Proper Q-mode input (hold Q → LMB/RMB for left/right).
3. Pickup montage + Anim Notify timing (can come after basic attach works).
4. Dual item highlight (nearby gray + targeted stronger) — lives in HeldItemsComponent for now.
5. Basic Extend (neutral vs extended anim soft refs).
6. Later: swinging, inventory, two-handed rules, damage, mass-based Physics Control strength, etc.

---

This document reflects the design state after the hold-system safety pass, left-hand mirroring, fragment initialization policy, and collision channel decisions of 2026-08-13.
