**Crafting System – Definition**
*(Updated 2026-09-03. Authority for how crafting works. Minigame catalog in §11. Fidelity + A2 build order in §16. TwoHanded slot: ItemAnimationPipeline §6.2.)*

ProjectVision.txt keeps the short system blurb and a link here. Item durability, hold/place, and factory rules stay in ItemSystemImplementation.txt. Station actors and sockets stay in the Structure system; this file only states what crafting needs from a station.

Content assets use `DA_Type_Name` (`DA_Recipe_SharpenedStone`, `DA_Minigame_Grind`, `DA_Item_Stone`, `DA_Structure_Workbench`).

Open sections are marked OPEN. Leave those until they are closed here.

Fidelity (read §16 before treating §11 as an A2 checklist):

- **Vision** — presentation stages / morphs, authored events (flip, crack), resume on selected long crafts as Items. Destination.
- **Shippable** — looks like the craft (presentation SKM morph and/or growing bones). Flip: later.
- **Core Loop / A2 MVP** — session + prompt + Factory consume/spawn. Grind motion on two live stones. Twist presentation. R / player damage returns the bound items.


### 1. Goals

- Crafting is how gathered items become tools, parts, materials, and later food-prep / metalwork products.
- Interaction stays manual: hold the right items, optionally park one on a station slot, get a quiet prompt, press the default action button, play a minigame.
- Every craft is a minigame, including simple ones. Types, movement modules, and meters are in §11.
- At most three item *types* in one recipe (two hands + one station slot). Longer chains are staged recipes (output of A is an input of B).
- Tools used in a craft are held. They are consumed only when the recipe says so.
- Minimal UI. One prompt, wheel to cycle matches, then a progress / minigame band in the same bottom-middle area.


### 2. Constraints

- A slot matches an exact item data asset or a gameplay tag on `UItemDefinition::ItemTags`.
- A hand that holds a stack (`count ≥ 2`) is not a legal craft socket. Unstack first (existing R + mouse rule).
- Hands craft is the absence of a station. The snapshot has no station in context.
- The recipe freezes when the session starts.
- A crafted hatchet is a new item definition (dual-mesh). Part-break stays on the Item durability design until that ships.


### 3. Context snapshot

Crafting reads a snapshot rebuilt when something relevant changes: hand attach / detach, Neutral vs Extended, look-at station, station slot contents.

Snapshot sockets:

- Left hand Instance (or Unarmed)
- Right hand Instance (or Unarmed)
- Station-in-context: the structure the player is looking at, or the structure that already holds the slotted item, plus that slot’s Instance (may be empty)

A snapshot does not match while:

- Either hand holds a stack
- Either hand is Extended — prompt and match list hide while any occupied hand is Extended
- A hand the recipe does not bind is occupied by a real item (that hand must be Unarmed; see §5.8)

Hands / ground craft: recipe `Station` unset, snapshot station empty.

Station craft: snapshot station identity equals the recipe’s station asset.


### 4. Pieces

Enums live in `WoodcraftTypes.h`:

- `ECraftingSlotRole` — Ingredient, Tool, Workpiece
- `ECraftingSlotMatchMode` — ExactDefinition, RequiredTag, Sharpenable
- `ECraftingMorphSampleMode` — Lerp, HoldAndPop
- `ECraftHintGesture` — None, Shake, StrikeDown, PushIn, CircleAlternate, CircleContinuous, PullAlong, HoldEngage

Structs live next to the type that owns them (`FCraftingSlot` / `FCraftingOutput` / snapshot / match on the recipe header because `FindMatches` lives there; session on the component header). Movement numbers live on the movement classes.

**`UCraftingRecipeDefinition`** (Primary Data Asset)
One asset per craft (`DA_Recipe_SharpenedStone`, `DA_Recipe_Cordage`, `DA_Recipe_StoneHatchet`, …).
`GetPrimaryAssetId` type: `CraftingRecipe`.

- `DisplayName` — used in the prompt
- `Icon` — selector / guidebook later
- `Station` — optional soft pointer to one station-identity asset (`UStructureDefinition` when Structures exist; `UPrimaryDataAsset` until then). Unset = hands craft. Set = that station only. Hands recipes stay hands-only.
- `Slots` — array of `FCraftingSlot` (usually 2, at most 3)
- `Outputs` — array of `FCraftingOutput` (one or more)
- `Stages` — `TArray<FCraftStage>` (length ≥ 1). Grind, Twist, and Strip author one stage. Tie authors three.

`FindMove<T>(int32 StageIndex)` on the recipe matches `UItemDefinition::FindFragment<T>()` against `Stages[StageIndex].Move`. The interpreter always asks for the session’s current stage.

**`FCraftingSlot`**

- `Role`: `Ingredient`, `Tool`, or `Workpiece`. A2 recipes use Ingredient.
- `Match`: exactly one of
  - `ExactDefinition` — this item data asset
  - `RequiredTag` — any item whose `ItemTags` contains the tag (`Item.Tool.Knife`, `Item.Fiber`, …)
  - `Sharpenable` — Hone only. Runtime: `UDurabilityItemFragment::bCanBeSharpened` and `CurrentHeadHealth < MaxHeadHealth`
- `bMustBeHeld`: `true` = must bind to a hand (tools). `false` = hand or the single station slot
- `bConsumed`: Factory-destroy that Instance on success
- `DurabilityCost`: `0` = none. On success, applied to the bound item if it has durability. Typical tools: kept, cost > 0 once wear ships

One instance per slot.

**`FCraftingOutput`**

- `ItemDefinition`
- `bAutoEquip`

A single-output recipe sets `bAutoEquip` true. Multi-output recipes flag at most one output. If `bAutoEquip` is false, that output piles.

**`FCraftPresentation`** (on the recipe header)

- `Mesh` — presentation SKM
- `Montage` — optional. Unset = spawn pose / first frame after intro

**`FCraftStage`**

- `Move` — one instanced `UCraftMovement`
- `Montage` — FPArms TwoHanded clip. Intro at the front, notify `IntroDone`, optional interactive tail
- `Presentations` — `TArray<FCraftPresentation>` spawned for this stage
- `MorphSampleMode` — `Lerp` or `HoldAndPop`
- `MorphChannelNames`
- `bGripIK_Left` / `bGripIK_Right` — Visible Control Rig gates for this stage
- `bHideLeft` / `bHideRight` / `bHideStation` — hide bound item renders (not presentation SKMs)
- `Hint` — `ECraftHintGesture` for the looping mouse-hint widget (widget ships later)

Same SKM asset may appear on more than one stage with a different presentation montage. Stage change tears down the current list and spawns the next.

**`UCraftMovement`** (abstract, `EditInlineNew`, `DefaultToInstanced`)
Data only. Same rules as item fragments: numbers and restrictions, no Tick.
Folder: `Public/Crafting/Movements/` (mirrors `Public/Items/Fragments/`).

`bProgressDrivesMontage` — after `IntroDone`, seek the FPArms TwoHanded clip from stage Progress. Default false. Wrap sets true when that class ships. Twist stays false and writes arms position from slices.

Concrete A2 classes:

- `UGrindActiveCraftMovement` — always in contact. Stroke `D`, travel volume, min speed (gate) / max speed (cap), planted vs working strengths.
- `UCircleAlternateCraftMovement` — animation / mouse slices. Slice radians, gesture radius.

Later classes (same naming, add when that craft ships): `UGrindPassiveCraftMovement`, `UGrabPullCraftMovement`, `UCircleContinuousCraftMovement`, `UStrikeCraftMovement`.

The interpreter on `UCraftingComponent` uses `FindMove<T>(Phase)` and runs that move. Movement objects do not run the session.

**`UCraftingComponent`** (on the player)
Owns:

- `RecipeAssets` — `TArray<TSoftObjectPtr<UCraftingRecipeDefinition>>` (A2 / MVP table)
- Snapshot rebuild and selected match index
- The live session
- The interpreter tick (one class, every minigame type)
- Start / cancel / commit
- Session presentation actor pointer

Matching: `UCraftingRecipeDefinition::FindMatches(const FCraftingSnapshot&, const TArray<UCraftingRecipeDefinition*>&)` returns `TArray<FCraftingMatch>` (recipe + slot bindings).

**`FCraftingSession`** (struct at the top of `CraftingComponent.h`)
Created on E, cleared on success, R, fail, or inbound player damage. Holds:

- Frozen recipe
- Frozen bindings (Item Instances / actors at start)
- Frozen pre-layout sockets (station crafts that swap an item onto the slot at E)
- Local stage Progress `0–1`
- `Phase` — 0-based stage index (notes say Stage 1..N)
- Engage state
- `bIntroActive`
- `IntroEndTime` — FPArms montage seconds at `IntroDone`
- Live presentation pointer (session-only)

The session is a struct on the component.

A2 / MVP file layout:

- `Public/Crafting/CraftingRecipeDefinition.h` + `Private/Crafting/CraftingRecipeDefinition.cpp`
- `Public/Crafting/Movements/CraftMovement.h`
- `Public/Crafting/Movements/GrindActiveCraftMovement.h` + matching `.cpp` if needed
- `Public/Crafting/Movements/CircleAlternateCraftMovement.h` + matching `.cpp` if needed
- `Public/Player/CraftingComponent.h` + `Private/Player/CraftingComponent.cpp` (player-only, same folder as HeldItems)
- Crafting enums in `Public/Core/WoodcraftTypes.h`
- `Public/Player/AnimNotify_CraftIntroDone.h` + matching `.cpp`
- `AWoodcraftCharacter` creates the component as a default subobject
- Content: `DA_Recipe_*` next to the primary output item.


### 5. Matching and binding

For each recipe, assign every slot to a snapshot socket.

1. `bMustBeHeld` slots consume hands first. Fail if not enough free hands.
2. Remaining slots fill leftover hands, then the station slot.
3. Each socket is used at most once.
4. A `Tool` slot is always `bMustBeHeld`.
5. Hands recipe: every slot lands on a hand. Station-in-context is empty.
6. Station recipe: station identity matches `Recipe.Station`. At least one slot has `bMustBeHeld == false` so the extra socket is usable.
7. Match test per slot: bound item’s definition == `ExactDefinition`, or item tags contain `RequiredTag`. Resume (later, selected recipes): a bound item with the in-progress marker for this recipe and `Progress < 1` also matches that slot.
8. A hand the recipe does not bind is Unarmed. Strip binds the stalk only; the Unarmed hand is the working hand.
9. A `Workpiece` role is the body that can become an in-progress item on a later resumable craft. At most one per recipe. Workpiece role: later resumable crafts.

Several recipes may match one snapshot. That is a list.

A2 two-hand working / planted split: Right = working / primary, Left = planted. Handedness settings can flip this later.


### 6. Prompt and selection

Shown only when both hands are Neutral and the match list length ≥ 1.

Bottom-middle, slightly transparent text, same band as stamina / future progress. Container-list language: selected row full; if `N > 1`, one neighbor above and below smaller and faded.

- `N = 1`: `[E] Craft {DisplayName}`
- `N > 1`: same line on the selected row; wheel moves selection and wraps
- Later resume: if a bound socket is an in-progress item for that recipe, the row reads `[E] Resume Crafting: {DisplayName}`

E is the default action button. The prompt shows the current key.

Mouse wheel while this prompt is up cycles matches. Same wheel as container UI; a hand Extended for container-take already hides this prompt.


### 7. Starting, cancelling, committing

**Start (E)**
Selected match freezes into a session. Gameplay IMC is removed and `IMC_Crafting` is added. Move, look, Q, swing, and stack-start are off with the gameplay IMC. View snaps `FPCamera` to `GroundCraftCameraTransform` and stays there. Prompt becomes `[R] Cancel`. Progress / minigame UI uses the same bottom-middle band later. Hint widget (later) sits under that line and reads the current stage’s `Hint`.

The recipe stays frozen for the life of the session. Snapshot rebuilds wait until the session ends. Instances are not rebound mid-session.

Station crafts that need a specific item on the slot (Tie: Stick) run a layout pass after freeze: if that item is in a hand and something else is slotted, swap those two actors with the same attach paths as Q place / retrieve. Remember the pre-layout sockets. Then `Phase = 0`, play Stage 1 intro if the arms montage has one, apply that stage’s grip-IK bools and hides.

**Cancel (R)**
Session cleared. IMC removed. Bindings return to the pre-layout sockets. A2 and every recipe that is not marked resumable leave the original items. Prompt may show again if the snapshot still matches.

Engage **release** pauses GrabPull and GrindPassive. The session stays up.

**Interrupt — any damage the player receives**
Same teardown as R. Immediate. Includes combat hits, fall damage, and any later damage channel that hurts the player.

`UCraftingComponent::NotifyOwnerDamaged()` is the hook. Player BP / future health code calls it.

Bound items stay in their sockets for the session. Q is off. Later multiplayer locks those sockets for other players until the session ends.

**Success**
Order:

1. Apply each slot’s `DurabilityCost` to the bound Instance
2. Factory-destroy every bound Instance with `bConsumed` (`DestroyHeldItem`: detach, `DestroyItemActorAndInstance`, EquipUnarmed)
3. Spawn each output via ItemFactory
4. Auto-equip at most one output (see §8)
5. Remaining outputs pile at craft origin as separate world actors
6. Clear session, remove IMC

**Fail**
A minigame may own a fail meter `0–1`. When it fills: session ends, tools stay, `DurabilityCost` is skipped, fail outputs pile if the recipe lists them.


### 8. Outputs, auto-equip, piles

**Craft origin**

- Hands craft: player location, in front / at feet (same idea as a drop)
- Station craft: station slot / work surface

**Auto-equip hand**

1. If any bound slot has `Role = Tool`, use the other hand
2. Else use the primary hand (Right until handedness settings exist)
3. That hand must be free after consumes
4. If auto-equip cannot run, that output piles instead

HeldItems `DestroyHeldItem(EHand)` consumes a real held item. `ReplaceHeldItem(EHand, AItemActor*)` auto-equips with no pickup montage. Factory `DestroyItemActorAndInstance(AItemActor*)` is the only destroy path, including Unarmed. `CompleteCraft()` runs this order. Auto-equip outputs spawn at `GetHeldSpawnTransform` (WeaponBone) with no rotation jitter. Piles use `GetDropTransform` with jitter. `bInstantComplete`: engage press calls `CompleteCurrentStage()` (last stage commits). Grind turns the flag off.


### 9. Stations (what crafting needs)

A station is a Structure. Crafting needs:

- A station identity: one structure data asset. The recipe’s `Station` field and the structure’s identity are the same asset
- Zero or one item slot on the shared socket frame already locked for items (`+Z` up, `+X` forward, item `PlacementOffsetTransform`)
- Detection: looked-at station, or the station that already owns the slotted item
- Player stand location and camera offset relative to the station

Q-place / retrieve is Item + Structure work. Typical station craft: park one ingredient with Q, stand Neutral with the other item(s) in hand, prompt appears, E.

A2 / MVP snapshots: empty station socket. Workbench and Tie ship in A3.

**Workbench (one-slot station, T1)**
Identity `DA_Structure_Workbench`. One actor from ghost to finished.

- Slot: marked flat patch on the top. Shared socket frame.
- Tie (`DA_Item_SharpenedStone` + `DA_Item_Stick` + `DA_Item_Cordage` → `DA_Item_StoneHatchet`) is the first bench recipe. One recipe, one session, three minigame stages (§11.7).
- Test level may spawn a finished bench (Roadmap A3). Player-built is A4.

Build graph lives in Notes/StructureSystem/StructureSystemDefinition.txt. BOM: 3 Logs, 2 Cordage, 5 Sticks.

**Grindstone (no-slot station, T3)**
First sharpenable tools are **T3 copper**. Identity `DA_Structure_Grindstone`.

- Snapshot: looking at the grindstone, one `Sharpenable` tool in a hand, other hand Unarmed, both Neutral. Prompt `[E] Sharpen {ToolName}`.
- Live mutate while Engage is held: `CurrentHeadHealth` rises at `R`, `MaxHeadHealth` falls at `r`. Release pauses writes; session stays up. R / damage ends the session and keeps writes already applied.
- `CurrentHeadHealth` reaches `MaxHeadHealth`: auto-release Engage, green flash, end session.


### 10. Staged crafts, unfinished items, progression

Three types is a hard cap per recipe. Longer *item* chains are several recipes. Several *motions* in one sitting are minigame stages on one recipe (§11.11).

T1 / Core Loop chain:

- `DA_Item_Stone` + `DA_Item_Stone` (hands, Grind, one stage) → `DA_Item_SharpenedStone`
- `DA_Item_FibrousStalk` + Unarmed (hands, Strip, one stage) → `DA_Item_PlantFiber` (Wood Scraps / Kindling: fire system)
- `DA_Item_PlantFiber` + `DA_Item_PlantFiber` (hands, Twist, one stage) → `DA_Item_Cordage`
- `DA_Item_SharpenedStone` + `DA_Item_Stick` + `DA_Item_Cordage` (workbench, Tie, three stages) → `DA_Item_StoneHatchet`

The hatchet is a new definition (StickHandle primary mesh + stone head secondary).

**Staged recipes**
Success of recipe A can spawn `UnfinishedX`. Recipe B consumes `UnfinishedX` and yields `X`. Two recipes. Example later: green bowl → fire harden. Separate from minigame stages.

**Resume — selected long crafts only (later)**
Twist, Tie, Strip, and A2 Grind complete in one sitting. R returns the original items.

Form (clay), Hammer (knap / forge), and other long shaping crafts may persist as **Items**:

- Snapshot already matches that recipe, and one bound item carries the in-progress marker fragment for that recipe.
- One in-progress mesh per resumable craft, or the fragment adds a skeletal mesh component at spawn. Choose when that craft ships.
- A point-of-no-return may Factory-replace the raw item with the in-progress item and disable R.
- Progress lives on that Item Instance as its own fields when this ships.

In-progress marker fragment + instance progress fields: later, Form / Hammer.

**Guidebook / unknown recipes — OPEN**
Prompt policy can follow the guidebook setting. Matching itself does not depend on known-state. Default lean: Discover (`[E] Craft ???` until learned).


### 11. Minigames

One interpreter on `UCraftingComponent`. Each recipe owns its `Stages` list. Types are data.

Audio and VFX are curated from meter events (engage, valid stroke / slice / pull / pop / finish, fail tick, authored events). Strike / knap / forge pops can spawn chips, sparks, or a wood burst at the hit. Niagara fields later.

#### 11.1 Camera and craft frame

Hands / ground: on E, set control pitch to 0 (`ArmsPivot` / arms level) and set `FPCamera` relative transform to `GroundCraftCameraTransform` on `UCraftingComponent` (scale forced to 1). Same transform for every ground craft. Locked default: location `25, 0, 65`, rotation `0, -50, 0` (pitch −50). Look stays off for the session. R restores control rotation and the camera relative transform. `FPCamera` parents on `ArmsPivot`. Camera `bUsePawnControlRotation` off. ArmsPivot on.

Station crafts: the station owns player stand location and camera offset relative to itself so eyes / arms land on a consistent frame. A3.

Deferred enter/exit (after A2 snap). Short “move to place and sit and/or look down.” Station: lerp player location and camera to the station craft pose. Ground: camera only, to `GroundCraftCameraTransform`. Beat 1: brief look-forward while hide flags apply and stage presentation SKMs spawn at AnimRef / ArmsPivot. Beat 2: lerp camera down onto the work surface. Short. R / session end blends back.

#### 11.2 Authority

`UCraftingComponent` owns the session, camera, type logic, engage button, progress / fail meters, and any presentation actor.

`UHeldItemsComponent` keeps Physics Controls and grip IK (`AnimRef` Hand relative to WeaponBone × item world). Crafting pushes motion into HeldItems.

HeldItems craft API:

- `BeginCraftMotion(Hand, LinearStrength, AngularStrength)`
- `SetCraftAxisLocks(Hand, LockX, LockY, LockZ)`
- `EndCraftMotion(Hand)`
- Guided TwoHanded slot play / `Montage_SetPosition` / stop — CraftingComponent on AnimRef (ItemAnimationPipeline §6.2). Stage intros use the same slot.
- Visible grip-IK bools per hand, written from the current stage

Swing orientation and collision damage stay off for the whole session. Player BP gates extend on `IsSessionActive()`. Start requires both hands Neutral. Start fails if the recipe has `Stages.Num() < 1`.

Stage change: `EndCraftMotion`, stop the previous TwoHanded montage, apply the new stage’s grip-IK bools and hides, spawn that stage’s `Presentations`, play that stage’s arms montage from 0. Intro plays with input off. `UAnimNotify_CraftIntroDone` on the FPArms clip (only) reports montage seconds and enables the stage’s move. Montage end with no notify also ends the intro.

Clocks:

- Intro: FPArms playhead is the clock. Each presentation montage is seeked to the same time (clamped to its own length). Presentations may animate into the working pose.
- After `IntroDone`: stage Progress is the clock. FPArms seek `lerp(IntroEndTime, ArmsLength, Progress)` only when `bProgressDrivesMontage`. Each presentation montage with a clip seeked `lerp(IntroEndTime, OwnLength, Progress)`. Presentation with no montage stays on spawn pose / first frame.
- Shared intro duration. Arms clip may be shorter than a presentation clip. `IntroDone` at the last arms frame leaves the arms held.

Progress may jump. Strike / knap / forge add `1/N` on a valid hit (`N` on `UStrikeCraftMovement` when that class ships). `HoldAndPop` holds the silhouette between steps. Valid-hit pops are also the VFX hook.

Live-item stages: working item transform comes from the interpreter. AnimRef clip is the place / brace pose. Visible Control Rig follows the live item on hands with grip IK on.

`bProgressDrivesMontage` stages: grip IK off. Visible copies AnimRef 1:1. Item Physics Controls stay planted. Twist writes arms position from slices instead of Progress; fiber presentations still sample Progress.

#### 11.3 Movement modules

Identity is the class. Look up with `FindMove<T>(Phase)`.

| Class | Used by | Motion |
|---|---|---|
| `UGrindActiveCraftMovement` | Sharpened Stone | Physics travel in a slab. Always in contact. |
| `UGrindPassiveCraftMovement` | Hone / grindstone | Physics. Click to engage. |
| `UGrabPullCraftMovement` | Strip, later Form / knife-spear | Mouse angle cone + pull distance. |
| `UCircleAlternateCraftMovement` | Cordage | Animation / opposite-direction slices. |
| `UCircleContinuousCraftMovement` | Tie Stage 3 wrap | Animation / continuous wrap. |
| `UStrikeCraftMovement` | Tie Stage 1 split; later knap / forge | Aim then slam. Hold-to-lock is session state. |

A2 authors GrindActive and CircleAlternate only. Each of those recipes has one stage. The component asks `FindMove<T>(Phase)` for the live stage and runs that class.

#### 11.4 Presentation and progress appearance

Some stages hide bound item render meshes and spawn presentation SKMs owned by `UCraftingComponent`. Bound Item Instances and their `AItemActor`s stay in their sockets until a later in-progress replace or a final consume.

Spawn presentation actors at AnimRef / ArmsPivot (Identity). Collision and Physics Control stay on the item actors.

Empty `Presentations` = live bound item meshes (Grind). Non-empty = spawn those SKMs. Montage vs morph targets is how the SKM is authored. Morph sample: `Lerp` or `HoldAndPop`.

Hide flags are per stage and hide live item renders: `bHideLeft` / `bHideRight` / `bHideStation`. Twist hides left and right. Grind shows both stones. Tie hides the slotted Stick the whole session, hides Cordage on Stages 1–2, hides the live stone on Stage 3.

`FCraftStage::Presentations` is the mesh list for that stage. Twist: one fiber SKM. Tie Split: stick SKM. Tie Seat: stick or stick+head SKM with a seat montage. Tie Wrap: wrap SKM plus seated-head SKM if it should stay visible (head has no montage if it should hold). Same SKM asset may repeat on a later stage with a different montage.

Item meshes stay without morphs. Shape change is presentation only.

**A2 presentation**

1. Session starts. Bindings are the existing item actors.
2. Spawn each `FCraftPresentation` on the current stage at AnimRef / ArmsPivot.
3. Stage hide flags → `SetVisibility(false)` on those bound primary meshes. Collision / simulate stay.
4. Intro: seek presentation montages to the FPArms playhead. After intro: seek / morph from session Progress.
5. Stage change or session end: destroy that list. Session end also unhides item actors that still exist.

The player holds `AItemActor` in A2.

#### 11.5 Tuning fields

On `FCraftStage`:

- Morph sample, morph channel names
- Move, arms Montage, Presentations, grip IK, hides, Hint

Optional fail-meter rate, events, checkpoints later on the recipe.

On the concrete movement:

- `bProgressDrivesMontage` on the base class
- GrindActive: stroke `D`, travel volume, min speed (gate) / max speed (cap), planted vs working strengths
- CircleAlternate: slice radians, gesture radius
- GrabPull (later): grab-start angle cone, pull direction, peel `D`
- GrindPassive (later): click engage, hone `R` / `r`
- Strike (later): strike slab, slam count `N`; lock-on-hold is session state

Exact numbers wait until the session exists; author placeholders and tune in PIE.

#### 11.6 Engage

Engage is per movement class. GrindActive is always in contact. GrindPassive clicks to engage. GrabPull uses Engage to grab. Strike uses the tool-hand mouse button for lock (LMB if the mallet is left, RMB if right).

#### 11.7 Types

**Grind** — one stage, `UGrindActiveCraftMovement`, hands, both grip IK on. `DA_Item_Stone` + `DA_Item_Stone` → `DA_Item_SharpenedStone`.

- Left (planted) is the workpiece: stone face up. Interpreter plants this hand.
- Right (working) is the abrader: stone face down. Only this hand is read for progress.
- Always in contact.
- Working hand XY motion inside the volume. Speed below min: no progress. Speed above max: clamped. A stroke is travel `D` then a reverse. Each valid stroke adds progress and a grind cue.
- A2: empty Presentations. Two cobbles the whole session. R / damage returns both stones. Success consumes both, spawns Sharpened Stone into the primary hand.

**Twist** — one stage, `UCircleAlternateCraftMovement`, hands, both grip IK off, `bHideLeft` + `bHideRight`. `DA_Item_PlantFiber` + `DA_Item_PlantFiber` → `DA_Item_Cordage`.

- One fiber presentation SKM at AnimRef / ArmsPivot: Y at the left pinch, two loose plies toward the working fingers, finished two-ply growing out the other side of the pinch as Progress goes `0 → 1`.
- Both hands planted, strengths near-max. Items stay put. Mouse does not travel a WeaponBone.
- Gesture: repeated opposite-direction circles in `GestureRadius`. Complete a CW slice of `Slice` radians, then a CCW slice, alternate. Mouse is the detector only.
- Arms: stage `Montage` (left pinch + right roll / swap) into AnimRef `TwoHanded`. `bProgressDrivesMontage` false. Interpreter writes `Montage_SetPosition` from the current slice. Fiber presentation samples Progress. Stop on teardown. Authored Left planted / Right working.
- A2: hands planted. Cues on slice and on length-up. TwoHanded slot Mirror later, with handedness settings.

**Strip** — one stage, `UGrabPullCraftMovement`, hands. `DA_Item_FibrousStalk` + Unarmed → `DA_Item_PlantFiber`.

- After the Grind + Twist PoC. Planted hand holds the stalk. Unarmed hand is the working / peel hand.
- Engage = grip a fiber strip. Valid pull of length `D` along the stalk axis adds progress.
- Success: consume stalk, spawn Plant Fiber. Wood Scraps / Kindling: fire system.

**Tie** — three stages, workbench, A3. One recipe. R returns Stick + Sharpened Stone + Cordage. Notes use Stage 1 / 2 / 3; `Phase` is 0 / 1 / 2.

Match: three Exact ingredients, any one may sit in the slot.

Layout at E: Stick onto the slot, Sharpened Stone in the working hand, Cordage in the other hand. Stick item actor stays in that slot for the session.

| Stage | Move | Picture |
|---|---|---|
| 1 Split | `UStrikeCraftMovement` | Stick presentation SKM (morphs or bones: mark / crack / split on slams). Live stone in the working hand. Left brace from the stage montage, left grip IK off. Right grip IK on the stone. Cordage hidden (`bHideStation` + hide the cordage hand). Aim circle on the stick end, engage when lined up, slam. Progress steps `1/N`. |
| 2 Seat | `UGrabPullCraftMovement` | Seat presentation SKM. Intro can animate the stick into the off hand. Prong bones scrub with local Progress as the stone seats. Live stone still visible. Right grip IK on. Cordage hidden. |
| 3 Wrap | `UCircleContinuousCraftMovement` | Hide the stone. Wrap presentation SKM (growing wrap) plus seated-head SKM if it should stay. Both grip IK off. `bProgressDrivesMontage` true. TwoHanded wrap montage; mouse is the detector. |

Stage complete: local progress `>= 1`. Last stage commits. Else `Phase++`, local `= 0`, play the next intro.

Exact slam count, morph names, and wrap SKM layout: author when the hatchet craft is built.

**Form** — `UGrabPullCraftMovement`, hands. Clay → green Clay Bowl. Later. Resumable.

**Hammer** — `UStrikeCraftMovement`, station. Flint knapping and forging. Later. Resumable.

**Flesh** — later hide-frame station. Movement class when that craft is authored.

**Hone** — `UGrindPassiveCraftMovement`, grindstone. T3 copper. Live Current/Max writes on the same tool Instance.

#### 11.8 Authored events (later)

A minigame may list events (`FCraftMinigameEvent`: trigger, id, optional arm montage, `bIntermission`). Flip moves the hand and the workpiece together. Flip: later.

#### 11.9 Recipe stage list

On every `UCraftingRecipeDefinition`:

- `Stages` — `TArray<FCraftStage>`, length ≥ 1
- Events / checkpoints / fail: optional

`FindMove<T>(Phase)` reads `Stages[Phase].Move`. Gesture numbers live on the movement class.

#### 11.10 Still parked

- Exact stroke, slice, peel, hone, Form, and Tie slam numbers
- Forge heat / knap map payloads
- TwoHanded slot Mirror when working hand is Left (handedness settings)
- AssistOffset two-hand IK for `bTwoHanded` items (ItemAnimationPipeline)
- Kindling vs Wood Scraps naming (fire system)
- In-progress item fragment + instance progress fields (Form / Hammer)
- Hint widget (field is on the stage)

#### 11.11 Stage machine

Notes say Stage 1..N. `FCraftingSession::Phase` is 0-based.

Each stage owns a local Progress `0–1`. Intro does not write progress. After `IntroDone`, the current move writes local progress.

```
local >= 1
  if Phase is last stage → CompleteCraft
  else Phase++, local = 0, apply next stage IK / hides / presentations, play next intro
```

`bInstantComplete` engage press calls `CompleteCurrentStage()` (same branch).

Grind, Twist, and Strip: one stage. Local `>= 1` commits.

Tie Cordage on the bench in Stages 1–2 is a hidden bound item or a presentation with no montage.


### 12. Input Mapping Context

Gameplay IMC is removed for the session. `IMC_Crafting` replaces it. Teardown restores the gameplay IMC.

`IMC_Crafting`:

- R → cancel
- LMB / RMB → `SetCraftEngage(EHand, bool)` (LMB = Left, RMB = Right)
- Move, look, Q, wheel off

Engage hand is resolved at E: a bound `Tool` slot’s hand, otherwise the default working hand (`Right` until handedness settings). The other mouse button is ignored.

A2 bindings stay on the Player BP and call:

- `TryStartCraft()` on E while the prompt is visible
- `CycleCraftMatch(int32 Delta)` on wheel while the prompt is visible (down = +1)
- `CancelCraft()` on R while a session is live
- `SetCraftEngage(EHand, bool)` on LMB / RMB while a session is live
- `NotifyOwnerDamaged()` from the player damage path when that path exists

Minigame mouse delta can join `IMC_Crafting` in A2.3 without giving the pawn look.


### 13. Item system contact points

- Create / destroy Instances only through `UItemFactorySubsystem` (`DestroyItemActorAndInstance`)
- Tags on `UItemDefinition::ItemTags`
- Durability numbers live on `UDurabilityItemFragment` / Instance state (ItemSystemImplementation.txt §10). Crafting applies `DurabilityCost` on success.
- Station place / retrieve: Q-mode, `PlacementOffsetTransform`, shared ReleasePose. Crafting reads the slot.
- Held stacks exist only in hands. `IsHandStacked(EHand)` can stub false until stacks ship.
- Unarmed test: held definition == `UHeldItemsComponent::UnarmedDefinition` (`DA_Item_Unarmed`)
- Snapshot dirty: `UHeldItemsComponent::OnHeldItemsChanged` after attach and after Neutral / Extended actually flips. Station look-at dirties the same way when A3 exists.


### 14. Example bindings

**Stone + Stone → Sharpened Stone** (hands, Grind, one output, auto-equip)
- Two Ingredient slots, Exact `DA_Item_Stone`, held, consumed, cost 0
- Snapshot: two Stones, both Neutral, hands craft
- Success: both consumed, `DA_Item_SharpenedStone` into primary, other hand Unarmed

**Plant Fiber + Plant Fiber → Cordage** (hands, Twist)
- Same two-ingredient shape with `DA_Item_PlantFiber`
- Presentation mesh during the session

**Fibrous Stalk → Plant Fiber** (hands, Strip) — after the Grind + Twist PoC
- Slot: Ingredient, Exact `DA_Item_FibrousStalk`, held, consumed
- Other hand Unarmed
- Output: `DA_Item_PlantFiber` (auto-equip)

**Stick + knife → Wood Shavings + Broken Stick** (hands, later)
- Slot A: Ingredient, Exact `DA_Item_Stick`, held, consumed
- Slot B: Tool, tag `Item.Tool.Knife`, held, kept, durability cost later

**Copper tool + grindstone → same tool, honed** (Hone, later)
- Station = `DA_Structure_Grindstone`
- One slot: `Sharpenable`, Role Tool, held, kept
- Other hand Unarmed going in

**Sharpened Stone + Stick + Cordage → Stone Hatchet** (workbench, Tie, A3)
- Station = `DA_Structure_Workbench`
- Three Ingredient slots, each exact def, `bMustBeHeld == false` so any one may sit in the slot
- Layout at E puts Stick in the slot
- One output, auto-equip primary
- All three consumed at CompleteCraft after Stage 3
- Minigame stages: Split / Seat / Wrap (§11.7)


### 15. Status

Locked this pass: snapshot, Neutral-only prompt, wheel select, E start, lock-on-E, R / player-damage cancel, GrindActive always in contact, recipe + slot + output + `Stages` fields, single station pointer, one instance per slot, consume / pile / auto-equip order, component owns table + session + interpreter, IMC swap, ground camera transform on the component, station pose on the station, engage = tool hand or default Right, `DA_Type_Name`, `UCraftingRecipeDefinition`, `UCraftMovement` instanced modules, AnimRef TwoHanded slot after L/R combine for Guided crafts.

Minigame stages (2026-09-05): ordered `FCraftStage` list on the recipe. Local `0–1` per stage; last stage commits, else `Phase++`. Morph sample + channel names live on the stage. `bProgressDrivesMontage` seeks the FPArms clip from Progress after intro. Presentations always sample Progress after intro. `IntroDone` lives only on the FPArms montage and reports time. Frozen bindings; layout pass may move actors onto the slot at E and R restores pre-layout sockets. Grind / Twist / Strip = one stage. Tie = three stages. Per-stage `Presentations` list. Item meshes stay without morphs.

A2 MVP recipes: Grind and Twist. Strip follows the PoC. Station / Tie is A3.

One interpreter. Presentation SKMs spawn at AnimRef / ArmsPivot.

Resume later is an Item on selected long crafts (Form, Hammer). A2 Grind / Twist / Strip return the original items on R.

OPEN: TwoHanded slot handedness Mirror, AssistOffset two-hand item IK, discovery prompt setting, hide result Instance vs new def, knap/forge payloads, exact numbers, Wood Scraps vs Kindling name, in-progress item fragment shape, hint widget, Tie slam count / morph names.


### 16. Fidelity tiers and A2 build order

**Core Loop / Roadmap A2 — implement in this order**

A2.0 First compile
- Recipe definition, `UCraftMovement` + `UGrindActiveCraftMovement` + `UCircleAlternateCraftMovement`
- Component (recipe table, snapshot, `FindMatches`, debug prompt)
- Enums in `WoodcraftTypes.h`
- Component default-subobject on `AWoodcraftCharacter`
- PIE: two Neutral Stones show `[E] Craft Sharpened Stone`

A2.1 Session
- `FCraftingSession` on E, IMC swap, `GroundCraftCameraTransform` snap on `FPCamera`, R / `NotifyOwnerDamaged()` teardown, wheel select, engage hand

A2.2 Factory complete
- `DestroyItemActorAndInstance` + HeldItems `DestroyHeldItem` / `ReplaceHeldItem` / `GetDropTransform`
- Success order in §7. `bInstantComplete` so two Stones become a Sharpened Stone before motion exists

A2.3 Grind
- HeldItems `BeginCraftMotion` / `EndCraftMotion`
- One-stage GrindActive. Empty Presentations. Always in contact. Volume + stroke `D` + speed gate/cap
- Interpreter `FindMove<T>(Phase)` (Phase stays 0)

A2.4 Twist
- Presentation list on the Twist stage. Hide both fibers
- Stage montage into AnimRef `TwoHanded` slot; arms position from slice; fiber SKM from Progress

Strip after that PoC plays (one stage, stalk + Unarmed → Plant Fiber only).

**Shippable (after the loop plays)**

- Grind workpiece `Morph` + `Lerp` follow-mesh on the planted stone. Abrader stays live cobble.
- Strip presentation SKM.
- Tie three-stage session + workpiece SKM + wrap SKM.

**Vision**

- Resume on Form / Hammer as Items with an in-progress marker and a dedicated mesh or added SKM.
- Flip and authored events.
- Hone unchanged (live writes on the same tool Instance).
